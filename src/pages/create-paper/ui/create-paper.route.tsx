import CreatePaperPage from "./create-paper.page"

import type { RouteObject } from "react-router"

export const createPaperRoute: RouteObject = {
  path: "/create-paper",
  element: <CreatePaperPage />
}
